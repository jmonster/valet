<h2 id="title">Valet</h2>

{{#if session.isAuthenticated}}
  <h3>Welcome, {{session.currentUser.handle}}</h3>
  <button {{action 'logout' }}>Logout</button>
{{else}}
  <button {{action 'login' 'twitter'}}>Login with Twitter</button>
  <button {{action 'login' 'google'}}>Login with Google</button>
  <button {{action 'login' 'facebook'}}>Login with Facebook</button>
  <button {{action 'login' 'github'}}>Login with GitHub</button>
{{/if}}

{{outlet}}
